resources:
  schemas:
    workshop_schema:
      name: ${var.schema}
      catalog_name: ${var.catalog}
      comment: Isolated schema for the DBX Agentic Development workshop

  volumes:
    workshop_landing:
      name: ${var.landing_volume}
      catalog_name: ${var.catalog}
      schema_name: ${resources.schemas.workshop_schema.name}
      volume_type: MANAGED
      comment: Deterministic synthetic payment landing data
