# Hosted rehearsal receipt — 2026-08-21

Status: `AUTHENTICATED / VALIDATION_PASS / COMPUTE_BLOCKED / CLEAN`

Target host: `https://dbc-ec01047b-2f32.cloud.databricks.com`  
Profile: `workshop-instructor`  
Bundle target: `dev`

## Proven

- OAuth authentication succeeded for the exact instructor host.
- `databricks bundle validate --strict -t dev -p workshop-instructor` succeeded.
- The first fresh-workspace attempt exposed and fixed an ordering defect: pipeline dry-run must follow deployment.
- That exploratory deployment exposed DAB development-mode schema prefixing. The final bundle uses the official
  `skip_name_prefix_for_schema` control; strict validation now resolves the schema to `dbx_agentic_dev` and the
  landing path to `/Volumes/workspace/dbx_agentic_dev/landing`, matching verification, Genie, and reset helpers.
- An earlier bundle design created the workshop schema, managed Volume, and pipeline, then both Job creates failed
  at `/api/2.2/jobs/create` with HTTP 403. Because Jobs are not a landing-page requirement, the final workshop
  removes that unnecessary dependency: dbldatagen runs locally, the CLI uploads its deterministic output, and DAB
  runs the pipeline directly.
- An isolated schema/Volume/pipeline deploy succeeded, but the pipeline dry-run failed to create compute with
  `RESOURCE_EXHAUSTED`.
- The existing `Serverless Starter Warehouse` (`38f2cc3fc22a0e6b`) is visible but cannot start. `SELECT 1` fails
  with `Cannot create the resource, please try again later.`
- The remote runner now checks that SQL runtime before deployment, writes a stage-specific failure receipt, clears
  stale evidence names, and destroys exact partial bundle resources on later-stage failures.
- The final guarded attempt stopped at `stage=preflight`; no deploy command ran in that attempt.

## Cleanup proof

After the exploratory deploys, the exact workshop resources were destroyed. Final read-only checks returned:

```text
PIPELINE_COUNT=0
SCHEMA_EXISTS=no
BUNDLE_PATH_EXISTS=no
```

No remote completion receipt exists. This is blocker evidence, not a successful hosted rehearsal.

## Required rerun

When compute becomes available, run:

```bash
export DATABRICKS_CONFIG_PROFILE=workshop-instructor
./scripts/preflight.sh --runtime
./scripts/e2e_remote.sh --confirm-remote --reset-and-restore
./scripts/readiness.sh
```

Release requires `PREFLIGHT_RUNTIME=READY`, `REMOTE_RESTORE=PASS`, `REMOTE_E2E=PASS`, live baseline/replay/Genie
evidence, nine accepted Task-Spec packets in `tasks/done/`, and `WORKSHOP_READINESS=READY`.

Databricks reference: [Free Edition limitations](https://docs.databricks.com/aws/en/getting-started/free-edition-limitations).
