# Local implementation receipt — 2026-08-21

Status: `LOCAL_PASS / FREE_EDITION_BLOCKED`

Source baseline: `bec30126d99bedae18ddd97af8a1e79a402a8d30` on `main`, with uncommitted workshop changes.

## Toolchain

- Databricks CLI: `1.13.0`
- PySpark and Spark Declarative Pipelines: `4.2.0`
- Java selected by the workshop wrapper: OpenJDK `21.0.12`
- DQX: `0.16.0`
- dbldatagen: `0.4.0.post1`
- Databricks Agent Skills catalog: `0.2.10`; selected set installed `7/32`

## Local proof

- `uv sync --frozen`: pass, 58 packages audited.
- `./scripts/verify.sh --local`: pass; Ruff and Spark graph dry-run. The final full suite passes `31` tests.
- `./scripts/e2e_local.sh`: pass; the baseline and replay were executed from fresh local warehouse/checkpoint
  state, then the active local pipeline was restored to baseline. Command logs are retained under ignored
  `.workshop-evidence/local/`.
- Full `./scripts/sdp.sh run --spec pipelines/spark-pipeline.yaml --full-refresh-all`: all 15 flows completed.
- Baseline rows: Bronze transactions `100000`, Silver transactions `98791`, transaction quarantine `1209`,
  dispute quarantine `1`, Gold merchants `12`.
- Baseline `m-007`: chargebacks `0`, quarantined transactions `1209`, rejected BRL `1204`, risk `25`, band
  `normal`.
- Local full replay: active disputes `101 → 102`, valid Silver disputes `100 → 101`; `m-007` chargebacks
  `0 → 1`, risk `25 → 45`, band `normal → elevated`.
- Fallback manifest restored to pre-replay state with logical digest
  `sha256:7f34ce150ac7fe391694efe6ace20a378e042c47ce9c66b8067509e837936d6d` and four
  semantic entity digests.
- Two independent Spark generations produced the same logical digest and the same four semantic entity digests.
- AgentSpec audit: 459 tracked legacy `.claude` files and the local `bypassPermissions` settings were removed;
  root `AGENTS.md`, `CLAUDE.md`, `.claude/README.md`, and seven official Databricks skill links remain.
- Task-Spec: nine packets minted; `LINT=OK`; dependency graph has zero structural issues. Retrospective
  per-task acceptance rejected the aggregate uncommitted change set on blast-radius attribution, so no packet was
  falsely moved to `tasks/done/`. Compatibility is blocked on hosted proof and its dependents remain open.

## Proof boundaries

- The local Spark runner uses a DQX-result-compatible projection because DQX 0.16.0 eagerly inspects schema
  inside its check application, which the local Spark declarative planner rejects. The Free Edition branch calls
  DQX directly. Therefore this receipt proves local routing and graph behavior, not hosted DQX execution.
- The local replay is a clean full replay because the open-source CLI catalog is process-local. Only the hosted
  replay upload followed by a direct DAB-managed pipeline update is incremental Lakeflow proof.

## Hosted boundary

Authentication and hosted attempts are now recorded separately in [hosted-2026-08-21.md](hosted-2026-08-21.md).
The instructor workspace is authenticated and bundle validation passes, but serverless compute is unavailable.
No completion receipt exists, so this local receipt remains `FREE_EDITION_BLOCKED` rather than release proof.
