# Rehearsal evidence

This directory receives sanitized full-live and forced-fallback receipts. A local implementation is not a
Databricks deployment receipt. Add run IDs, update IDs, timestamps, durations, and cleanup state only after an
authorized rehearsal.
